import styled from 'styled-components';


const StyledWbPlayer = styled.div`

    background: ${props => props.theme.bgcolor};
    border : ${props => props.theme.border};
    max-width: 1000px;
    margin: 0 auto;
    
`;

export default StyledWbPlayer;